// ─────────────────────────────────────────────────────────────
// js/ai-config.example.js — ต้นแบบไฟล์ตั้งค่าคีย์ AI (คัดลอกไปเป็น js/ai-config.js)
//
// ⚠️ คำเตือน: โปรเจกต์นี้ห้ามทำเซิร์ฟเวอร์ของเราเอง (ตามสเปก) ดังนั้นคีย์นี้ต้องถูกฝังไว้ใน
// โค้ดฝั่งเบราว์เซอร์ — ใครก็ตามที่เปิด DevTools ของเบราว์เซอร์ก็จะเห็นคีย์นี้ได้ทันที
// จึงไม่ใช่ความลับที่ปลอดภัยจริง ๆ ควรสร้างคีย์ที่ "ตั้งเพดานวงเงินใช้จ่ายต่ำ" (spending limit)
// ไว้ที่หน้าบัญชี openrouter.ai เพื่อจำกัดความเสียหายหากมีคนเอาคีย์ไปใช้ต่อ
//
// วิธีตั้งค่า:
//   1. สมัคร/ล็อกอินที่ https://openrouter.ai แล้วสร้าง API key ใหม่ (ตั้งวงเงินต่ำไว้ก่อน)
//   2. คัดลอกไฟล์นี้เป็นชื่อ js/ai-config.js (ไฟล์นั้นถูก .gitignore กันไว้แล้ว จะไม่ถูก push)
//   3. แทนที่ค่า placeholder ด้านล่างด้วยคีย์จริงของคุณ
//   4. ห้าม commit/push ไฟล์ js/ai-config.js ที่มีคีย์จริงขึ้น GitHub เด็ดขาด
// ─────────────────────────────────────────────────────────────

export const OPENROUTER_API_KEY = "ใส่ API key จาก openrouter.ai ตรงนี้";
export const MODEL = "openai/gpt-4o-mini";
